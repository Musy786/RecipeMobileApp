<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RecipeApp.Pages.MainPage"
             Title="Home">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
            
            <Label Text="Welcome to RecipeApp!"
                   FontSize="30"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Label Text="Your personal recipe collection."
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   TextColor="Gray" />

            <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="20,10" />

            <Button Text="View All Recipes"
                    Clicked="OnViewAllRecipesClicked"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="White"
                    CornerRadius="8"
                    FontSize="16"
                    HeightRequest="50" />

            <Button Text="Add New Recipe" 
                    Clicked="OnAddNewRecipeClicked"
                    BackgroundColor="{StaticResource Tertiary}"
                    TextColor="White"
                    CornerRadius="8"
                    FontSize="16"
                    HeightRequest="50" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>